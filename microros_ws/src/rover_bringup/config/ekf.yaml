ekf_filter_node:
  ros__parameters:
    frequency: 30.0
    two_d_mode: false      # <--- CORRECTED: Must be FALSE to unlock Z, Roll, and Pitch
    publish_tf: true       # (Note: Requires a fix in tick_to_odom.cpp, see below)

    map_frame: map
    odom_frame: odom
    base_link_frame: base_link
    world_frame: odom

    # --- SENSOR 1: Wheel Odometry ---
    odom0: /odom
    # Feed X-velocity and Yaw-velocity from the filtered wheels
    odom0_config: [false, false, false,
                   false, false, false,
                   true,  true, false,
                   false, false, true,    # <--- CORRECTED: Now fusing Vyaw (angular.z)
                   false, false, false]

    # --- SENSOR 2: IMU ---
    imu0: /imu/data
    # We feed the EKF everything except absolute X/Y/Z position.
    imu0_config: [false, false, false,
                  true,  true,  true,     # <--- Roll, Pitch, Yaw
                  false, false, false,
                  true,  true,  true,     # <--- Angular Velocities (X, Y, Z)
                  false, false, false]
    
    # --- SENSOR 3: Visual Odometry from RTAB-Map ---
    odom1: /vo
    # We fuse relative visual velocities (the second row of 6 booleans)
    odom1_config: [false, false, false,
                   false, false, false,
                   true,  true,  true,   # <--- Fuse Visual Vx, Vy, Vz
                   true,  true,  true,   # <--- Fuse Visual Vroll, Vpitch, Vyaw
                   false, false, false]

    imu0_remove_gravitational_acceleration: true
    